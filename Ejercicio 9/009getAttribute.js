"use strict"

 //seleccionamos el div 
let div = document.querySelector("div");

//mostramos el valor que del atributo llamado "data-widget-name", se hace asi ya que no es un atributo estandar
alert(div.getAttribute("data-widget-name")); 